<?php

// Application configuration
// Change these values to set your login, password and database path

// Simple credentials for backend authentication
$LOGIN_USERNAME = 'admin';
$LOGIN_PASSWORD = 'password';

// Path to the SQLite database file used by the API
// Default points to the project-level db.sqlite next to this src folder
$DATABASE_PATH = __DIR__ . '/../../db/db.sqlite';
